TODO:
1. Implementar LS-12 Manage invalid operations
2. Que pasa si hago close / read de un DIR * NULL (en principio está protegido). No creo que sea necesario meter test
3. Añadir valgrind a los tests E2E
4. Mensaje de error de stat
5. Estado de error de parsed args (stat) en caso de ser file operand
6. Estado de error de parsed args (stat) en caso de NO ser file operand
7. Tests parsed args y application stat
8. Mirar posibles refactors que me haya dejado
9. Revisar requerimientos y apuntarlos más claramente (comportamiento local con 1 fallo, comportamiento global, errores faltantes...)
11. Estado de error de scan (opendir + readdir + closedir) en caso de ser file operand
12. Estado de error de scan (opendir + readdir + closedir) en caso de NO ser file operand


TODO En algún momento:
- Investigar si los tests están acoplados a la implementación interna de las cosas
- Refactor renderer, donde render llama a render_strategy o lo que sea basado en options
- Reflexionar si meter el should print header en render pasando parsed arguments. Si al hacer las options se necesita
  pasar parsed arguments al render, entonces calcularlo ahi
- Para manejar errores de cualquier tipo, mirar la conversación de: https://chatgpt.com/c/6a711028-5c44-83eb-a7a4-e50ff2877990


TODO Para mejorar siguientes proyectos:
- Las historias no pensarlas tan prematuramente (o tener más cosas en cuenta)
- No ser tan rígido con el número de ellas, ya que algunas son muy grandes porque he tenido que hacer muchas cosas en ella porque no tenía
el terreno allanado, como con los mocks, github actions, etc. Cada una de ellas podía ser un ticket de Jira (tarea, no historia) diferente.
- TDD hacerlo bien: escribir código del test del caso más sencillo, código mínimo para que pase, refactor
- Pensar mejor las historias (una historia que es "manage errors" no es una buena historia)
- Flujo:
  - Buscar al principio las fuentes necesarias para saber todos los requerimientos. TODAS, cuanto más oficiales mejor (POSIX, etc.)
  - Apuntar todos los requerimientos y separarlos por épicas (basic behavior, mandatory options...)
  - Reordenar los requerimientos de cada épica en historias
- Mirar el casing de C
- Mirar si en C se suele usar bools o simplemente ints


TODO QA:
- Investigar de nuevo casos completos (para esa historia, no épica)
- Probar que este hecho, tanto a mano como con tests



QA LS-8 Execution without parameters (`ls -aU | cat`)
1. Directorio vacío - NO ACCESIBLE (siempre hay '.' y '..')
2. Un solo fichero - NO ACCESIBLE (siempre hay '.' y '..')
3. Múltiples ficheros
4. Ficheros con nombres raros se ven bien
5. No memory leaks


QA LS-11 Execution with parameters (`ls -aU file ... | cat`)
1. 1 fichero
2. 1 directorio
3. 2+ ficheros
4. 2+ directorios
5. Mezcla ficheros + directorios
6. No memory leaks


QA LS-15 Filter hidden files (`ls -U file ... | cat`)
1. Sin parámetros (directorio actual)
   - Con y sin hidden files
   - Vacío / Solo hidden files
2. 1 parámetro
   - Si es un fichero
     - Normal y oculto
   - Si es un directorio
     - Con y sin hidden files
     - Vacío / Solo hidden files
3. Múltiples parámetros
   - Si solo hay ficheros
     - Solo normales, solo ocultos y mezcla
   - Si solo hay directorios
     - Con y sin hidden files
     - Vacíos / Solo hidden files
   - Si hay mezcla
     - Con y sin hidden files
     - Vacíos / Solo hidden files
   - Mezcla de lo anterior y comprobar dir headers
4. Parámetros ocultos (ficheros y directorios | .env .idea/)
5. No memory leaks


QA LS-14 Default sorting output (`ls file ... | cat`)
1. Sin parámetros
2. 1 parámetro (directorio)
3. Múltiples parámetros
   - Archivos
   - Directorios
     - Que se ordene nombres y contenidos
   - Mixto
     - Que se ordenen ambos y por separado
5. No memory leaks


QA LS-13 Support for multiple types of files
- Disclaimer: Al listar contenido de un directorio, se escribe el nombre de los ficheros contenidos, incluso
              si es un symlink (no del target)
1. Sin parámetros
2. 1 parámetro
   - Archivo cualquiera
   - Symlink archivo cualquiera
   - Directorio
   - Symlink directorio
   - Symlink directorio con barra al final (linkdir/) -> E2E only
3. Múltiples parámetros
   - Mezclar combinaciones anteriores (symlink archivo / directorio, archivo normal...)
   - Asegurarse que los dir headers están bien
   - Si es un symlink -> directorio, el dir header es el nombre del link
4. No memory leaks


QA LS-12 Manage invalid operations
1. Opendir + readdir + closedir. Note: los parámetros que fallan son solo directorios
   - Sin fallos (sin parámetros, 1 parámetro de cada tipo, múltiples parámetros)
   - 1 Fallo:
       * Sin parámetros (current dir): No se puede abrir, falla leer a la primera o a la mitad, no se cierra
       * 1 parámetro: No se puede abrir, falla leer a la primera o mitad, no se cierra
       * Mezcla de parámetros:
           · 2 params: Mezclar entre parámetro que falla y que no (no_dir + dir y dir + dir)
           · X params: Mezclar entre parámetro que falla y que no (X dirs, X non_dirs, 1 fallo)
   - X Fallos:
       * 2 params, 2 fallos: Mezclar entre no poder abrirse, fallos de lectura, fallos al cerrar
       * X params, X fallos: Mezclar entre fallos y successes (X dirs, X non_dirs, N fallos)


To test in stat:
1. ls in a current directory without permissions:
    - ls: cannot open directory '.': Permission denied
    - ls: cannot access '..': Permission denied
